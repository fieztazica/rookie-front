td.table-cell.border.px-6.py-4
    span.flex.space-x-2
        a(href=`/admin/${resourceName}/${row.id}`) View
        a(href=`/admin/${resourceName}/${row.id}/edit`) Edit
        a(href=`/admin/${resourceName}/${row.id}/delete?from=list`) Delete
